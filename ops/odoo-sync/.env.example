# ── Supabase ───────────────────────────────────────────────────────────────────
# URL de ton projet (ex: https://abcdsupabase.supabase.co)
SUPABASE_URL=

# Clé API à privilégier côté serveur :
# - idéalement la SERVICE_ROLE (si tu écris/updates et as besoin de bypass RLS).
# - sinon une clé avec les droits requis sur les tables cibles.
SUPABASE_KEY=

# ── Odoo ───────────────────────────────────────────────────────────────────────
# URL XML-RPC d’Odoo (ex: https://odoo.mondomaine.com)
ODOO_URL=

# Nom de la base Odoo
ODOO_DB=

# Identifiant de l’utilisateur Odoo (souvent un email)
ODOO_USER=

# Mot de passe ou token d’API pour cet utilisateur
ODOO_PASSWORD=

# ── Exécution ─────────────────────────────────────────────────────────────────
# Mettre "true" pour simuler (aucune écriture), "false" pour exécuter réellement
DRY_RUN=true

# (Optionnel) Filtre temporel (ex: ne traiter que depuis une date)
# SINCE=2025-01-01

# (Optionnel) Niveau de log: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# (Optionnel) Timeouts / retries
# REQUEST_TIMEOUT=30
# MAX_RETRIES=3

# (Optionnel) SSL strict pour Odoo: "true" (par défaut) ou "false" si cert self-signed
# ODOO_VERIFY_SSL=true
