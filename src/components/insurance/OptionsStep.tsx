import { InsuranceFormData } from "@/types/insurance";
export const OptionsStep = ({ formData, updateFormData }: any) => (
  <div className="text-center py-10 text-muted-foreground">Aucune option disponible pour ce tarif.</div>
);
